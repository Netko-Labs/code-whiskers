import { type ChatMessage, chatMessageTable } from '@code-whiskers/{{ name }}-domain'
import { db } from '@code-whiskers/{{ name }}-repository'
import { desc } from 'drizzle-orm'

export const getChatMessages = async (limit = 100): Promise<ChatMessage[]> => {
  const messages = await db
    .select()
    .from(chatMessageTable)
    .orderBy(desc(chatMessageTable.createdAt))
    .limit(limit)
  return messages.reverse()
}
